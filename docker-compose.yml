services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: profitpulse
      POSTGRES_USER: profitpulse
      POSTGRES_PASSWORD: profitpulse_dev
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
